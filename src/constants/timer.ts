export const REST_EXTEND_SEC = 15;
export const NEW_EXERCISE_MIN_REST_SEC = 60;
export const TIMER_TICK_MS = 250;
export const ELAPSED_TICK_MS = 1000;
/** Warning sound when this many seconds of rest are left. */
export const REST_WARNING_SEC = 10;
export const COUNTDOWN_TICK_SEC = 3;
/** Rest that ended longer ago than this (app was in background) closes silently — the notification already alerted. */
export const STALE_ALERT_MS = 2500;
export const REST_NOTIFICATION_CHANNEL = 'rest-timer';
export const REST_VIBRATION_PATTERN = [0, 300, 150, 300];
